@RestController
public class ApiDeomApplicationTest{
    String trans = "handle";
    public static void main(String[] args) {
        user contractName
        contractAddress funcName
        contractAbi funcParam
        groupId useCns
        SpringApplication.run(ApiDeomApplication.class, args);
    }
}